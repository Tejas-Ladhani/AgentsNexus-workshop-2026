# MCP Server Lab

Use the official Python MCP SDK documentation at https://py.sdk.modelcontextprotocol.io/ and the Model Context Protocol specification at https://modelcontextprotocol.io/specification/latest when modifying this lab server.

Keep the server local-only and use its fixed demonstration data. Do not add real credentials, external integrations, or destructive operations.